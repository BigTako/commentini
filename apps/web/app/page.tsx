"use client";

export default function Page(): JSX.Element {
  // const { data } = useQuery({
  //   queryKey: ["get-hello"],
  //   queryFn: () => getHello(),
  // });

  return <main>sheesh</main>;
}
